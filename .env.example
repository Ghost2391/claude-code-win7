# Claude Code 通过 settings.json 的 env 字段配置环境变量。
# 将配置放在 ~/.claude/settings.json（用户全局）或 .claude/settings.json（项目级）：

# {
#   "env": {
#     "CLAUDE_CODE_USE_OPENAI": "1",
#     "OPENAI_API_KEY": "your-api-key-here",
#     "OPENAI_BASE_URL": "http://your-api-host:port/v1",
#     "OPENAI_MODEL": "your-model-name"
#   }
# }

# 原 .env 文件已不再支持，请使用 settings.json 的 env 字段替代。
